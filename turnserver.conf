# Simple TURN server config for testing
listening-port=3478
external-ip=127.0.0.1
realm=xrcupid.local
server-name=xrcupid.local

# Simple static credentials
user=test:test123

# Enable verbose logging
verbose

# Allow all IPs
no-stun-backward-compatibility
